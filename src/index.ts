// Startup script, add breakpoints to test debugging

let strings: string[] = [];

strings.push("Hello");
strings.push("World!");

strings.forEach(str => {
  console.log(str);
});

console.log("Done");
